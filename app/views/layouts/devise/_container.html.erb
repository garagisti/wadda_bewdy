<div class="container-fluid">
  <div class="row">
    <div class="col-sm-4 col-sm-offset-4 main">
      <!-- SDP: I think your flash needs some padding -->
      <%= render 'shared/flash' %>
      <%= yield %>
    </div>
  </div>
</div>
